# https://docs.zizmor.sh/configuration/
rules:
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
  secrets-outside-env:
    config:
      allow:
        - DASHBOARD_GITHUB_TOOLS_TOKEN
